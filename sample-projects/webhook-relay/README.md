# webhook-relay (sample project)

Deliberately vulnerable/flawed Python code, written only to be scanned by SonarQube
so the DevSecOps Portal has real code-quality data to display. Do not run or deploy this.
